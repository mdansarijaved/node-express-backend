/**
 * This is a Node.js TypeScript template using SWC. 
 * To use this template, simply click on the "Use this template" button on GitHub and install dependencies using `pnpm i`.
 * Make sure to put all the code inside the `src` folder.
 */

console.log("Hi from index.ts");
console.log("use the command 'npm run dev' to run the typescript files");
console.log(
  "use the command 'npm run build' to compile the typescript files to javascript files"
);
console.log("use the command 'npm run prod' to run the javascript files");
console.log(
  "make sure to add .js in the import statements while importing from ts files, not required for node_modules"
);
// import { test } from "./test.js";

